import 'package:where_is_it/zz_stateholder/stateconfig.dart';

class WiiState extends StateConfig {
  // define color theme
  // define other global stuff like version from app and so on
  final String APPVERSION = "0.0.1";
}
