import 'package:flutter/material.dart';

class PButtons {
  static simpleButton(BuildContext context, Function onPress) => TextButton(
        onPressed: () {},
        child: const Text('SAFE'),
      );
}
